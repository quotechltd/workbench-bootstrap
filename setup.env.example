# Workbench Development Environment Configuration
# Copy this file to setup.env and fill in your details
# This file should be placed in the workbench monorepo root directory
# Run the setup script from the monorepo root: ./setup-dev-environment.sh

# ==============================================================================
# REQUIRED: Git Configuration
# ==============================================================================

# Your full name for Git commits
GIT_USER_NAME="Your Name"

# Your email address for Git commits
# This should match your GitHub email
GIT_USER_EMAIL="your.email@example.com"

# ==============================================================================
# OPTIONAL: GitHub Authentication
# ==============================================================================

# GitHub Personal Access Token (PAT) for automated authentication
# If not set, you'll be prompted to authenticate via browser
# Create a token at: https://github.com/settings/tokens
# Required scopes: repo, read:org, workflow
# GITHUB_TOKEN=""

# ==============================================================================
# OPTIONAL: Package Manager
# ==============================================================================

# Use pnpm instead of npm for frontend dependencies (default: false)
# Set to "true" to use pnpm
USE_PNPM="false"

# ==============================================================================
# OPTIONAL: Bootstrap Configuration
# ==============================================================================

# Bootstrap mode: "local" or "uat"
# - local: Creates fresh local data using bootstrap script (default)
# - uat: Clones data from UAT environment (requires UAT access)
BOOTSTRAP_MODE="local"

# ==============================================================================
# OPTIONAL: UAT Database Configuration (only needed if BOOTSTRAP_MODE="uat")
# ==============================================================================

# UAT PostgreSQL connection details
# UAT_DB_HOST="uat-postgres.example.com"
# UAT_DB_PORT="5432"
# UAT_DB_NAME="workbench"
# UAT_DB_USER="workbench_owner"
# UAT_DB_PASSWORD="your-uat-password"

# UAT Zitadel connection details
# UAT_ZITADEL_URL="https://uat-zitadel.example.com"

# Service account credentials (recommended for automation)
# Option 1: Personal Access Token (PAT) - Recommended, simpler
# UAT_ZITADEL_SERVICE_USER="service-account@org.zitadel.cloud"
# UAT_ZITADEL_SERVICE_KEY="your-personal-access-token-here"  # Long alphanumeric token from PAT

# Option 2: OAuth Client Credentials (if you have client secret instead of PAT)
# UAT_ZITADEL_SERVICE_USER="service-account@org.zitadel.cloud"
# UAT_ZITADEL_SERVICE_KEY="your-client-secret"

# ==============================================================================
# OPTIONAL: Bootstrap User Configuration (only used if BOOTSTRAP_MODE="local")
# ==============================================================================

# These are passed to the bootstrap script as flags
# Leave empty to use defaults

# Superadmin user details (defaults shown)
# BOOTSTRAP_FIRSTNAME="Local"
# BOOTSTRAP_LASTNAME="Superadmin"
# BOOTSTRAP_EMAIL="local.superadmin@quotech.io"
# BOOTSTRAP_PASSWORD="Password1!"

# Client user details (defaults shown)
# BOOTSTRAP_CLIENT_FIRSTNAME="Demo"
# BOOTSTRAP_CLIENT_LASTNAME="Client"
# BOOTSTRAP_CLIENT_EMAIL="demo.client@example.com"
# BOOTSTRAP_CLIENT_PASSWORD="ClientPass1!"

# ==============================================================================
# NOTES
# ==============================================================================

# This script must be run from the workbench monorepo root directory
# Expected structure:
#   workbench/
#     ├── backend/
#     ├── frontend/
#     ├── setup-dev-environment.sh
#     └── setup.env
#
# After running the setup script:
# 1. The FRONTEND_ENV_PATH will be automatically added to your ~/.zshrc
# 2. GitHub authentication:
#    - If GITHUB_TOKEN is set, authentication is automatic
#    - Otherwise, you'll authenticate via browser during setup
# 3. Docker Desktop will be installed and configured automatically
# 4. Host networking will be enabled automatically in Docker Desktop
